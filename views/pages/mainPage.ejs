<!DOCTYPE html>
<html>
<head>
  <%- include ("../partials/header.ejs") %>
</head>

<body>
  <div class="container">
    <div class="row mt-3">
      <div class="col d-flex flex-column justify-content-center align-items-center">
        <img src="./logo.png" alt="" class="logo">

        <h3 class="mt-2 text-center">Отчетность для клиентов и аналитика по успеваемости для тренеров</h3>
      </div>
    </div>
    <div class="row d-flex auth__block my-4">
      <div class="col-sm-6 mt-4">
        <h4>Ученик? Введите ID</h4>
          <div class="mb-3">
            <label for="studentId" class="form-label">ID ученика</label>
            <input number="student_id" type="number" class="form-control" id="studentId" aria-describedby="">
            <div class="form-text">Введите ID</div>
          </div>
          <button onclick="getStudent()" type="submit" class="btn btn-primary">Перейти</button>
      </div>

      <div class="col-sm-6 mt-4">
        <h4>Сотрудник ФМЦИ? Перейти в систему</h4>
        <form action=<%= `${globalLink}/users/login/` %> method='POST'>
          <div class="mb-1">
            <label for="inputEmail" class="form-label">Email address</label>
            <input name="email" type="text" class="form-control" id="inputEmail" aria-describedby="emailHelp">
            <div id="emailHelp" class="form-text">Введите почту</div>
          </div>
          <div class="mb-3">
            <label for="InputPassword" class="form-label">Пароль</label>
            <input name="password" type="password" class="form-control" id="InputPassword">
          </div>
          <button type="submit" class="btn btn-primary">Перейти</button>
        </form>
      </div>
    </div>
  </div>

  <%- include ("../partials/footer.ejs") %>

<script>

const getStudent = () => {
  const student_id = document.getElementById("studentId").value;
  window.location = `${window.location.href}views/${student_id}`;
}

</script>